name: Retired GitHub live autotrade

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: retired-github-live-autotrade
  cancel-in-progress: false

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the permanent cutover
        run: |
          echo "GitHub-based live ordering is permanently retired."
          echo "Use the protected AWS runtime after its dry-run acceptance gates pass."
